# Prints

The live catalog now uses shared Google Drive thumbnails from `data/print-catalog.json`.

Use this folder only for optional local reference art that should be committed intentionally.

Recommended filename format:

```text
character-name-size.jpg
```

After adding a new catalog item, update the shared Google Sheet and regenerate `data/print-catalog.json` instead of editing `server.js`.
